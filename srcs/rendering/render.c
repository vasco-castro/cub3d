
#include "cub3d.h"

void	render(void)
{
	// size_t x, y;

	// y = 0;
	// while (W_HEIGHT / 2 >= y++)
	// {
	// 	x = 0;
	// 	// while (W_WIDTH >= x++)
	// 		// mlx_pixel_put(game()->mlx, game()->win, x, y, game()->ceiling);
	// }
	// while (W_HEIGHT >= y++)
	// {
	// 	x = 0;
	// 	while (W_WIDTH >= x++)
	// 		// mlx_pixel_put(game()->mlx, game()->win, x, y, game()->floor);
	// }
}